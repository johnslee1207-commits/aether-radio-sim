# fpga-emulator

Remote Radio FPGA emulator: `IQSource`, `SlotScheduler`, `Packetizer`, timestamp/sequence generators.

Timing parameters: `configs/radio_timing.yaml`.

```bash
cargo test -p fpga-emulator
```
