version: "1.0.0"
id: soak-mvp
classification: project_specific
description: L4 soak / stress profile — long symbol run with optional stress faults
bench_profile: configs/bench_profile.yaml
fault_config: configs/fault_injection_stress.yaml
symbol_count_override: 2048
streams_override: 2
report_path: data/reports/soak_last.json
events_path: data/reports/soak_events.jsonl
max_sequence_gaps: 64
max_deadline_miss_ratio: 0.5
min_packets: 1000
rounds: 2
require_health_normal: false
