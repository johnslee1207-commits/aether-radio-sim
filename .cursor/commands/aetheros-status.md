# /aetheros-status

Purpose: report project status without mutating implementation.

Output: current stage, milestone, passed gates, unresolved blockers, pending decisions, latest evidence bundle, remaining budget, recommended next action.
